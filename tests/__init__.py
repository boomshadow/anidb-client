"""Test suite for anidb-client.

A package rather than a bare directory so that `tests.conftest` is importable by
name, and so test modules in different subdirectories may share a basename
without colliding in sys.modules.
"""
